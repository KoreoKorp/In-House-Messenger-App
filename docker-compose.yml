services:
  messenger:
    build: .
    container_name: jeans-messenger
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:3002:3000"
    networks:
      - proxy-nw

networks:
  proxy-nw:
    external: true
